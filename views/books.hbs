{{>header }}
<div class="full-row animated lightSpeedIn">
    {{#each displayDataBooks}}
    <div class="item-wrapper-outside">
        <div class="item-wrapper-inside">
            <div class="item-img">
                <img src="{{imageLink}}" />
            </div>
            <div class="details">
                <h5>Title:{{this.title}}</h5>
                <h5>Author:{{this.author}}</h5>
                <h5>Uploaded by:{{this.user.email}}</h5>
            </div>
            <div class="buttons">
                <a href="/books/swap?book_id={{this._id}}" class="swap-button">SWAP</a>
                <a href="/books/edit?book_id={{this._id}}" class="edit-button">EDIT</a>
                <a href="/books/remove?book_id={{this._id}}" class="remove-button">DELETE</a>
            </div>
        </div>
    </div>
    {{/each}}
</div>
{{>footer }}